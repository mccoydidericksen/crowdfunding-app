<div class="row align-center mb-5" style="display: flex; flex-direction: column; flex-wrap: wrap; gap: 15px;">
  <div class="col-md-6">
    <h2> {{project.name}}</h2>
  </div>
  <div class="col-md-6">
    <h3> Description: {{project.description}}</h3>
  </div>
  <div class="col-md-6">
    <h3>Date Created: {{format_date project.date_created}}</h3>
  </div>
  <div class="col-md-6">
    <h3>Funding Needed: {{project.needed_funding}}</h3>
  </div>
</div>